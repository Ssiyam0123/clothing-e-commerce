import CartPage from "@/modules/client/cart/pages/CartPage";

export const metadata = {
  title: "Cart | Vanguard",
  description: "Review your selected premium streetwear artifacts before authorization.",
};

export default function CartRoute() {
  return <CartPage />;
}
